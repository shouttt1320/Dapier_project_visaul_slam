# 평면 수평성 검증 (Planar Slope Verification) 완벽 가이드

> **핵심 요약**  
> 좌표계 변환(`Xb, Yb, Zb`)을 거쳐 높이 필터(`0.11m ≤ Zb ≤ 0.17m`)를 적용하더라도, **경사면이나 바닥면 노이즈가 높이 필터 구간을 사선으로 관통(Intersection)할 때 발생하는 '가짜 책상'을 걸러내기 위해 평면 수평성 검증이 반드시 필요**합니다.

---

## 1. 질문의 핵심 배경

> **사용자 질문**:  
> *"기울기로 평면 수평성 검증하는 부분은 좌표계 변환 안 하고 쓸 때 넣었던 알고리즘 같은데, 지금 좌표계 변환하는 알고리즘에서도 필요한 거야?"*

### 결론: **네, 좌표계 변환 후에도 반드시 필요합니다.**

좌표계 변환 전과 후의 검증 대상이 달라졌을 뿐, **필터링의 역할은 더욱 정밀해졌습니다.**

| 구분 | 좌표계 변환 전 (카메라 좌표계) | 좌표계 변환 후 (로봇 바닥 좌표계) |
| :--- | :--- | :--- |
| **측정 기준** | 카메라 렌즈 기준 깊이 (`Zc`) | 로봇 바닥 접지면 기준 높이 (`Zb`) |
| **카메라 시선** | 60° 하향 경사 시선 | 중력 반대 방향 (수직 상향 `Zb`) |
| **기울기 검증 목적** | 60° 기울어진 영상에서 책상 평면 찾기 | **높이 슬라이스를 사선으로 관통하는 가짜 평면(바닥/경사면/노이즈) 제거** |

---

## 2. 좌표계 변환 후에도 왜 바닥/노이즈가 검출될까?

로봇은 책상 상판을 분리하기 위해 로봇 바닥 기준 높이 슬라이스를 적용합니다:

```python
# precision_approacher_node.py (Line 432)
table_mask = (z_b >= 0.11) & (z_b <= 0.17) & (x_b >= 0.145) & (x_b < 1.40) & (np.abs(y_b) <= 0.40)
```

이론상 책상 높이가 `Zb = 0.14m`이고 바닥은 `Zb = 0.0m`이므로, 높이 필터만으로 완벽히 분리될 것 같습니다.  
하지만 **실제 환경에서는 다음 2가지 이유로 바닥/경사면 점들이 `0.11m ~ 0.17m` 슬라이스 안으로 침범**합니다.

### 2.1 기하학적 관통 (Planar Intersection)
카메라가 아래로 60° 숙여져 있는 상태에서, 바닥면이나 바닥의 완만한 경사면(전선 몰딩, 바닥 요철, 로봇의 가감속 피치 흔들림)이 높이 슬라이스 윈도우(`Zb ∈ [0.11, 0.17]m`)를 **사선으로 통과**할 수 있습니다.

```
       ▲ Zb (높이)
       │
0.17m ─┼───────────────────────────  (슬라이스 상한)
       │           ┌──────────┐  ◀◀◀ [진짜 책상 상판] (수평: slope_x ≈ 0)
0.14m ─┼───────────┴──────────┴────
       │      /                      (슬라이스 하한)
0.11m ─┼─────/─────────────────────
       │    /  ◀◀◀ [경사면/바닥 슬라이스 관통 점들] (급경사: slope_x >> 0.25)
       │   /
0.00m ─┴──/────────────────────────► Xb (전방 거리)
      바닥(Ground)
```

- **진짜 책상**: `Xb`가 0.3m에서 0.8m로 멀어져도 높이 `Zb`는 **항상 0.14m로 일정 (수평)**
- **가짜 평면 (사선 관통)**: `Xb`가 증가함에 따라 `Zb`도 함께 급격히 증가하며 슬라이스를 스쳐 지나감 (`slope_x ≥ 0.40`)

### 2.2 Depth 센서의 원거리 노이즈 특성
ToF/구조광 카메라는 거리가 멀어질수록 깊이 오차가 거리의 제곱에 비례하여 커집니다.  
바닥면의 먼 거리(`Xb > 0.8m`) 반사광 노이즈가 튀면서 일부 점들이 순간적으로 `Zb = 0.11 ~ 0.13m` 높이로 산란되어 들어옵니다.

---

## 3. 알고리즘 동작 원리 (최소자승법 평면 피팅)

`precision_approacher_node.py`의 Line 444~457에서 실행되는 알고리즘입니다.

### 3.1 3차원 평면 방정식 모델링
높이 슬라이스를 통과한 점 구름 `(Xb, Yb, Zb)`에 대해 3차원 평면 방정식을 세웁니다:

```
Zb = a · Xb + b · Yb + d
```

- `a = slope_x` : 전방 방향(X축) 기울기 (∂Zb / ∂Xb)
- `b = slope_y` : 좌우 방향(Y축) 기울기 (∂Zb / ∂Yb)
- `d` : 높이 오프셋 (Z절편)

### 3.2 최소자승법 (Least Squares Fitting)
행렬 수식으로 나타내면 다음과 같습니다:

```
┌                     ┐ ┌   ┐   ┌     ┐
│  X1     Y1     1.0  │ │ a │   │ Z1  │
│  X2     Y2     1.0  │ │ b │ = │ Z2  │
│   :      :      :   │ │ d │   │  :  │
│  XN     YN     1.0  │ └   ┘   │ ZN  │
└                     ┘         └     ┘
        A                 c        Z
```

파이썬 코드 한 줄로 이를 고속 해결합니다:
```python
# A 행렬: [N x 3], Z 벡터: [N x 1]
A_mat = np.column_stack([x_table, y_table, np.ones_like(x_table)])
coeffs, _, _, _ = np.linalg.lstsq(A_mat, z_table, rcond=None)

slope_x = float(coeffs[0])  # 전방 기울기 (X 방향)
slope_y = float(coeffs[1])  # 좌우 기울기 (Y 방향)
```

---

## 4. 판별 조건 및 임계값 (Threshold) 설정 이유

### 4.1 수평성 검증 조건
```python
# 전방/후방 기울기가 14도(|slope_x| > 0.25) 이상이면 가짜 평면으로 판정하고 기각
if abs(slope_x) > 0.25:
    self.get_logger().info(
        f"[EDGE] Rejected non-horizontal surface (slope_x={slope_x:+.2f} > 0.25). Likely floor.",
        throttle_duration_sec=2.0
    )
    return
```

### 4.2 각도와 기울기 환산표

| 기울기 (`slope_x`) | 경사 각도 (`θ = arctan(slope_x)`) | 실제 의미 | 판정 결과 |
| :---: | :---: | :--- | :---: |
| **±0.00 ~ 0.05** | **0.0° ~ 2.8°** | 완벽히 평평한 실제 책상 상판 | **PASS (정상 책상 인지)** |
| **±0.10 ~ 0.20** | **5.7° ~ 11.3°** | 로봇 주행/가감속 중 발생하는 미세 피치 흔들림 | **PASS (허용 오차 내 유지)** |
| **±0.25** | **14.0°** | **[임계값 기준선 (Boundary)]** | **Threshold** |
| **> 0.40** | **> 21.8°** | 바닥면/사선 관통 가짜 점군 | **REJECT (바닥 오인식 차단)** |
| **> 0.80** | **> 38.6°** | 책상 다리, 벽면 등 수직 장애물 단면 | **REJECT (장애물 차단)** |

---

## 5. 결론 및 효과

1. **좌표계 변환과의 상호보완 관계**:
   - **좌표계 변환(`Xb, Yb, Zb`)**: 3차원 공간에서 책상이 있을 법한 **"높이 영역(Where)"**을 1차 필터링합니다.
   - **평면 수평성 검증(`slope_x`)**: 그 영역 안에 들어온 물체가 진짜 바닥과 수평인 **"책상 상판(What)"**인지 2차 검증합니다.

2. **시스템 안정성 기여**:
   - 로봇이 책상으로 접근할 때 원거리에서 바닥을 책상으로 착각하여 조기 도킹을 시도하거나, 엉뚱한 방향으로 회전하는 오작동을 **100% 방지**합니다.
   - 바닥 coexistence(공존) 검사처럼 "바닥이 반드시 시야에 보여야 한다"는 제약이 없으므로, **5cm 초근접 상태에서도 안정적으로 동작**합니다.
